apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: shopcart
spec:
  selector:
    app: user-service
  ports:
    - protocol: TCP
      port: 5001          # Port other services call
      targetPort: 5001    # Port the container listens on
  type: ClusterIP         # Internal only — not exposed to internet